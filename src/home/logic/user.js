'use strict';
import fs from 'fs-promise';

/**
 * logic
 * @param  {} []
 * @return {}     []
 */
export default class extends think.logic.base {
  /**
   * index action logic
   * @return {} []
   */
  indexAction(){
   
  }

  async loginAction(){
    
  }

  async editAction(){
    if (this.isPost()) {
      
      // this.success(file)
    }
  }
}